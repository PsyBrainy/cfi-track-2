// depositEntry.js — punto de entrada de deposit.html.
// Inicializa el menú de navegación (hamburguesa responsive).
// La lógica de depósito se mantiene en deposit.js (script clásico).

import { initNav } from './nav.js';

document.addEventListener('DOMContentLoaded', initNav);
