// config.js — configuración central compartida
// Url Base del backend. En local apunta a localhost:8080.
// Si el proyecto se sube a un servidor, cambiar esta URL.
export const BaseUrl = 'http://localhost:8080';

// API pública de Georef (datos.gob.ar) para provincias y ciudades
export const GEOREF_BASE = 'https://apis.datos.gob.ar/georef/api';
